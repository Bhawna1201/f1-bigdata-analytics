docker-compose up -d
This starts all 7 containers.

Step 2 — Check containers:
bashdocker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
 containers are running.

Step 4 — Check Airflow:
Open http://localhost:8081 (login: admin / admin)
Step 5 — Check Spark:
Open http://localhost:8080


Step 1 — Wait 30 seconds, then open:
http://localhost:8081
Step 2 — If it doesn't load, check logs:
docker logs f1_airflow_web --tail 30

Stale PID file is blocking Airflow. Fix it:
docker exec f1_airflow_web rm -f /opt/airflow/airflow-webserver.pid
docker restart f1_airflow_web

AIzaSyDhBnOrKCTCacZ4l5XAku4PTBlWKif0BmQ

cd ~/Documents/3rd_sem/f1_bigdata
source venv/bin/activate

streamlit run dashboard/app.py

*********
check data in Bronze layer 
python -c "
import pandas as pd
df = pd.read_parquet('data/bronze/ergast/race_results_2026.parquet')
print(df[['season','round','race_name']].drop_duplicates().sort_values('round'))
"
python ingestion/silver_transform.py
python ingestion/gold_features.py
python ml/integrate_idle_data.py
python ml/model_comparison.py
python agents/agentic_pipeline.py